---
const { text,link } = Astro.props;
---

<a href={link} class="text-lg bg-[var(--colorPrincipal)] px-4 py-2 rounded-lg transition-all duration-500 hover:bg-[var(--hoverColorPrincipal)] cursor-pointer">{text}</a>

