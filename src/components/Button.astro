---
const { text,link } = Astro.props;
---

<a href={link} class="bg-[var(--colorPrincipal)] px-6 py-2 rounded-lg transition-all duration-500 hover:bg-[var(--hoverColorPrincipal)] cursor-pointer xl:text-lg md:text-xl">{text}</a>

