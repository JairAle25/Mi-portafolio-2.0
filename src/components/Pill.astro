---
const {className,url,name} = Astro.props
---

<div class=`flex justify-center items-center gap-1 ${className} rounded-lg py-1`>
    <img src={url} alt="" class=" w-[12%]">
    <p class="font-semibold text-sm">{name}</p>
</div>

